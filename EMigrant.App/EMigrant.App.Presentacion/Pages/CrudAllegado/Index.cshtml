@page
@model EMigrant.App.Presentacion.Pages.CrudAllegado.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Allegado[0].EmigranteId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Allegado[0].IdAllegado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Allegado[0].NombreAllegado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Allegado[0].IdentificacionAllegado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Allegado[0].TipoAllegado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Allegado) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EmigranteId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdAllegado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NombreAllegado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdentificacionAllegado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoAllegado)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
